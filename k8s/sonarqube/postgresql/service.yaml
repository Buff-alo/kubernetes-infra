apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: sonarqube
spec:
  ports:
    - port: 5432
      name: postgres
  clusterIP: None   # Headless service — allows direct DNS for StatefulSet
  selector:
    app: postgres
